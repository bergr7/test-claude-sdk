[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    unit: Unit tests using mocked Claude SDK components
    integration: Integration tests with real Claude API or tool calls
    e2e: End-to-end workflow tests
    expensive: Tests that cost money to run (API calls, external services)